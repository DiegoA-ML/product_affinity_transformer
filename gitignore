# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# Optuna study database (regenerated by the training notebook)
*.db
*.sqlite3

# macOS
.DS_Store

# Editor
.vscode/
.idea/

# NOTE: model artifacts (*.pt, *.pkl) are intentionally NOT ignored —
# they are required for the API to run and are committed to the repo.
